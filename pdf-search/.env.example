# ========================================
# PDF Search Configuration Template
# ========================================
#
# RECOMMENDED: Use GitHub Secrets instead of .env file
# See GITHUB-SECRETS-SETUP.md for detailed instructions
#
# This .env file is for LOCAL DEVELOPMENT ONLY
# For web-based Claude Code, set secrets via GitHub:
#   https://github.com/YOUR_USERNAME/sociology/settings/secrets/actions
#
# Priority: GitHub Secrets > .env file > defaults
# ========================================

# ====================
# REQUIRED (Sensitive)
# ====================
# Set this as GitHub Secret: PINECONE_API_KEY
PINECONE_API_KEY=your_pinecone_api_key_here

# ====================
# OPTIONAL (Non-Sensitive)
# ====================
# These have sensible defaults but can be customized

# Pinecone Settings
PINECONE_ENVIRONMENT=us-east-1  # or your region
PINECONE_INDEX_NAME=sociology-pdfs
PINECONE_NAMESPACE=default  # or organize by learning unit (LE_I, LE_II, etc.)

# Embedding Configuration
EMBEDDING_MODEL=llama-text-embed-v2  # Pinecone serverless embedding
EMBEDDING_DIMENSION=1024  # depends on model

# PDF Processing
CHUNK_SIZE=1000  # characters per chunk
CHUNK_OVERLAP=200  # overlap between chunks
MAX_CHUNKS_PER_PDF=1000  # safety limit

# Search Configuration
DEFAULT_TOP_K=5  # number of results to return
SIMILARITY_THRESHOLD=0.7  # minimum similarity score

# ====================
# SETUP INSTRUCTIONS
# ====================
# For local development:
#   1. Copy this file: cp .env.example .env
#   2. Edit .env with your actual API key
#   3. Never commit .env to git (already in .gitignore)
#
# For web-based usage (GitHub Secrets):
#   1. Get Pinecone API key from pinecone.io
#   2. Add as GitHub secret (Settings → Secrets → Actions)
#   3. Name: PINECONE_API_KEY
#   4. Done! No .env file needed
